import 'package:flutter/material.dart';

class ErrorMesaage extends StatelessWidget {
  const ErrorMesaage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('Oops there was an error , try agian later..');
  }
}
